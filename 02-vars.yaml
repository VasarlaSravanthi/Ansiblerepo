- name: Demo on variables
  hosts: Local
  connection: local
  vars_files: 
   -course.yaml
  tasks: 
   - name: Print variables
     debug:
     msg: "I am Leaning {{COURSE}} from {{TRAINER}} with {{DURATION}}"
